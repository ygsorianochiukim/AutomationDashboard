import { DashboardOverview } from './dashboard-overviewmodel';

describe('DashboardOverview', () => {
  it('should create an instance', () => {
    expect(new DashboardOverview()).toBeTruthy();
  });
});
