export interface DashboardOverview {
    conversation_log_id?: number,
    customer_psid?: number,
    conversation_status?: string,
    conversation_updated_from?: string,
    created_by?: number | null,
}
